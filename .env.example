# =============================================================================
# ENVIRONMENT VARIABLES GUIDE
# =============================================================================
# Copy this file to .env for local development
# For Vercel deployment, add these in your Vercel project settings

# -----------------------------------------------------------------------------
# CLIENT-SAFE VARIABLES (prefixed with VITE_)
# -----------------------------------------------------------------------------
# These are exposed to the browser and are safe to be public

# Supabase Public Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# -----------------------------------------------------------------------------
# SERVER-ONLY VARIABLES (NO VITE_ prefix)
# -----------------------------------------------------------------------------
# These are NEVER exposed to the browser - keep them secret!

# Database Connection (contains credentials - NEVER expose to client!)
DATABASE_URL=postgresql://postgres.your-project:password@aws-0-us-east-2.pooler.supabase.com:6543/postgres

# Supabase Service Key (has admin privileges - NEVER expose to client!)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Google Maps API (server-side geocoding)
GOOGLE_MAPS_API_KEY=AIzaSyC...

# Optional: Payment Processing
# STRIPE_SECRET_KEY=sk_test_...
# VITE_STRIPE_PUBLIC_KEY=pk_test_...

# Optional: Email Services  
# SENDGRID_API_KEY=SG...

# =============================================================================
# SECURITY RULE: 
# - VITE_ prefix = client-safe (visible in browser)
# - No VITE_ prefix = server-only (never visible in browser)
# =============================================================================